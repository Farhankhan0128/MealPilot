# MealPilot Prototype

This is a dependency-free static prototype for the Swiggy Builders Club access demo.

Run it locally:

```bash
python3 -m http.server 5173
```

Open:

```text
http://localhost:5173
```

The current prototype uses seeded responses. The next implementation step is to connect the same UI states to a local MCP-compatible dev stub.
